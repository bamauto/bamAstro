---
import { PageLayout, LocalBusinessSchema } from '@bamastro/ui';
import { region } from '@/config/region';

export function getStaticPaths() {
  return [
    { params: { region: region.id } },
  ];
}

const { region: regionId } = Astro.params;
const venueType = region.venueTypes.find(v => v.id === 'karaoke');

if (!venueType) {
  throw new Error(`Venue type 'karaoke' not found in region config`);
}

const seoProps = {
  title: `${region.name} 가라오케 예약·추천 | 가격·시스템 완벽 가이드`,
  description: venueType.description,
  keywords: venueType.keywords,
};
---

<PageLayout {...seoProps} region={region}>
  <LocalBusinessSchema region={region} />
  
  <div class="py-12">
    <!-- Header -->
    <div class="mb-12 text-center animate-fade-in-up">
      <span class="inline-block px-3 py-1 rounded-full bg-pink-500/10 text-pink-300 text-sm font-medium mb-4 border border-pink-500/20">
        Luxury Party
      </span>
      <h1 class="text-4xl lg:text-5xl font-bold text-white mb-4">
        {venueType.name} <span class="text-gradient">Guide</span>
      </h1>
      <p class="text-xl text-slate-400">
        {venueType.subtitle}
      </p>
    </div>

    <div class="grid lg:grid-cols-3 gap-10">
      <!-- Main Content -->
      <div class="lg:col-span-2 space-y-12">
        <section class="glass p-8 rounded-2xl animate-fade-in-up delay-100">
          <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-1 h-8 bg-pink-500 rounded-full"></span>
            Facility
          </h2>
          <div class="prose prose-invert prose-lg max-w-none text-slate-300">
            <p>{venueType.description}</p>
            <p>
              최신 음향 시설과 화려한 조명, 넓고 쾌적한 룸 컨디션을 자랑합니다.
              생일파티, 뒤풀이, 각종 모임에 최적화된 공간을 제공합니다.
            </p>
            <ul class="grid sm:grid-cols-2 gap-4 list-none pl-0 mt-8">
              <li class="flex items-center gap-3 bg-white/5 p-4 rounded-lg">
                <span class="text-pink-400">🎵</span> TJ 최신 반주기
              </li>
              <li class="flex items-center gap-3 bg-white/5 p-4 rounded-lg">
                <span class="text-pink-400">🎤</span> 무선 마이크 시스템
              </li>
              <li class="flex items-center gap-3 bg-white/5 p-4 rounded-lg">
                <span class="text-pink-400">📺</span> 100인치 대형 모니터
              </li>
              <li class="flex items-center gap-3 bg-white/5 p-4 rounded-lg">
                <span class="text-pink-400">🚽</span> 룸 내 전용 화장실
              </li>
            </ul>
          </div>
        </section>

        <section class="glass p-8 rounded-2xl animate-fade-in-up delay-200">
          <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-1 h-8 bg-purple-500 rounded-full"></span>
            Party Package
          </h2>
          <div class="bg-gradient-to-br from-indigo-900/40 to-purple-900/40 p-6 rounded-xl border border-white/10">
            <h3 class="text-xl font-bold text-white mb-4">생일/모임 이벤트</h3>
            <p class="text-slate-300 mb-4">
              사전 예약 시 특별한 혜택을 드립니다. 케이크 세팅 및 파티 용품 대여 가능합니다.
            </p>
            <ul class="text-sm text-slate-400 space-y-2">
              <li>• 고급 샴페인 1병 서비스 (4인 이상)</li>
              <li>• 파티룸 데코레이션 무료</li>
              <li>• 단체 픽업 서비스 제공</li>
            </ul>
          </div>
        </section>
      </div>

      <!-- Sidebar -->
      <div class="space-y-6 lg:sticky lg:top-24 h-fit animate-fade-in-up delay-300">
        <div class="glass p-6 rounded-2xl border border-pink-500/30 shadow-[0_0_30px_rgba(236,72,153,0.1)]">
          <h3 class="text-xl font-bold text-white mb-6 text-center">Price Info</h3>
          <div class="space-y-4 mb-8">
            <div class="flex justify-between items-center pb-4 border-b border-white/5">
              <span class="text-slate-400">주대 (12년)</span>
              <span class="text-lg font-bold text-white">{venueType.minPrice.toLocaleString()}<span class="text-xs font-normal text-slate-500 ml-1">원~</span></span>
            </div>
             <div class="flex justify-between items-center pb-4 border-b border-white/5">
              <span class="text-slate-400">맥주 세트</span>
              <span class="text-lg font-bold text-white">100,000<span class="text-xs font-normal text-slate-500 ml-1">원</span></span>
            </div>
            <div class="flex justify-between items-center pb-4 border-b border-white/5">
              <span class="text-slate-400">TC (시간당)</span>
              <span class="text-lg font-bold text-white">50,000<span class="text-xs font-normal text-slate-500 ml-1">원</span></span>
            </div>
          </div>
          
          <div class="space-y-3">
            <a href={`tel:${region.phone}`} class="btn-primary w-full group">
              <span class="relative z-10">전화 예약하기</span>
            </a>
            <a href={region.kakaoLink} target="_blank" class="btn-secondary w-full text-center block">
              카카오톡 문의
            </a>
          </div>
        </div>

        <div class="bg-pink-900/20 p-6 rounded-2xl border border-pink-500/20">
           <p class="text-sm text-pink-200 text-center font-medium">
             "최고의 음향 시설로 당신의 노래 실력을 뽐내보세요!"
           </p>
        </div>
      </div>
    </div>
  </div>
</PageLayout>
